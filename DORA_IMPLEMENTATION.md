## ドラ実装完了レポート

### 実装日時
2026年2月19日

### 実装内容

ドラ（役牌）の機能をマージャンゲームに実装しました。以下の要件を満たしています：

1. **ドラで翻数が増える**
   - ドラ表示牌の次の牌が手牌にあると、その枚数分翻数が増加
   - 例：ドラ表示牌が「北」の場合、「白」（次の牌）が手牌の役として加算

2. **ドラだけでは和了できない（重要なルール）**
   - 役がドラだけの場合は和了不可
   - 他の役（平和、一気通貫、リーチなど）とドラを組み合わせることで初めて和了可能

3. **リーチで和了したときは裏ドラが乗る**
   - リーチ宣言後のツモ和了時に裏ドラが適用
   - 表ドラと同様の方式で計算

### ファイル修正内容

#### 1. MahjongLogic.js

**修正ポイント：**
```javascript
// calculateWinScoreメソッドにドラ情報を追加
const scoreResult = this.scoreCalculator.calculateScore({
  // ... 既存の情報
  doraIndicators: this.doraIndicators,
  doraTiles: this.doraTiles,
  urahaTiles: player.riichi ? this.getUrahaTiles() : []
});

// 新規メソッド：裏ドラ取得
getUrahaTiles() {
  // リーチで和了した場合、ドラ表示牌の下の牌（裏ドラ）を返す
}
```

#### 2. ScoreCalculator.js

**追加メソッド：**

```javascript
// ドラをカウント
countDora(hand, doraIndicators, doraTiles) {
  // 手牌からドラを見つけてカウント
  let doraCount = 0;
  const actualDoraTiles = [];
  doraIndicators.forEach(indicator => {
    const nextTile = this.getNextTile(indicator);
    actualDoraTiles.push(nextTile);
  });
  // 手牌の中でドラを探してカウント
}

// ドラ表示牌の次の牌を取得
getNextTile(tile) {
  // 1-9 → 2-9, 9 → 1
  // honor 1-7 → 2-7, 7 → 1
  const nextNumber = tile.number === 9 ? 1 : 
                    (tile.number === 7 && tile.suit === 'honor' ? 1 : tile.number + 1);
  return new Tile(tile.suit, nextNumber);
}
```

**修正内容：**

- `calculateScore`メソッド：ドラ情報を受け取るようにシグネチャ拡張
- `detectYaaku`メソッド：
  - ドラをカウントして役に追加（`isDora: true`フラグ付き）
  - 裏ドラ（リーチツモ時）も計算
  - ドラのみの場合は役なしとして扱う処理を追加

### テスト結果

#### test-dora-implementation.js

すべてのテストに成功：

```
【テスト1】ドラのカウント
✅ ドラ表示牌「北」→ ドラ「白」を正しく計算
✅ 手牌内のドラ2枚を正しくカウント

【テスト2】ドラを含む役判定
✅ 一気通貫 + ドラ(2枚) = 4翻で和了成功
✅ 得点：8000点（満貫）

【テスト3】ドラのみでは和了できない確認
✅ バラバラの手牌でドラのみの場合は和了不可

【テスト4】リーチツモ時のドラと裏ドラ
✅ リーチ、ツモ、一気通貫、ドラで6翻
✅ 得点：12000点（跳満）
```

#### test-cpu-battle.js

- ✅ CPU同士の対戦が正常に完了
- ✅ エラーなく最後までゲーム進行
- ✅ ゲームロジック全体で問題なし

### ドラの計算ルール

1. **基本**
   - ドラ表示牌（ドラインジケーター）の次の牌が「実ドラ」
   - 例：表示「一萬」→ 実ドラ「二萬」

2. **数字の循環**
   - 数字系（萬・筒・索）：1-2-3-...-8-9-1
   - 字牌（東南西北白發中）：東-南-西-北-白-發-中-東

3. **ドラの翻計算**
   - ドラ1枚 = 1翻
   - 複数枚の場合は枚数分加算

4. **リーチ時の裏ドラ**
   - リーチ宣言時に見せられるドラ表示牌の下の牌が「裏ドラ」
   - ツモ和了時のみ適用
   - ロン和了時は不適用

### 麻雀ルール遵守

✅ **重要なルール**
- ドラだけでは和了できない（他の役が必須）
- リーチ宣言後の和了時に裏ドラが乗る
- ドラはツモ・ロン両方で対象

### 今後の拡張可能性

- 多重ドラ（複数の局でドラが設定される場合）
- 赤牌（アカ）の実装
- カン時の追加ドラ表示

---

**実装完了日**：2026年2月19日
**テスト確認日**：2026年2月19日
