# ツモ運モード（Tsumo Luck Mode）

## 概要

ツモ運モードは、プレイヤーのツモ（牌の引き）に幸運の補正をかけるオプション機能です。プレイヤーごとに3段階（0=補正なし、1=軽い補正、2=強い補正）で調整でき、引きやすい牌をより多く獲得できる確率を高めることができます。

## 機能説明

### ツモ運レベル

- **レベル0（補正なし）**: 牌をランダムに引きます
- **レベル1（軽い補正）**: 30%の確率で実用的な牌を優先的に引きます
- **レベル2（強い補正）**: 70%の確率で実用的な牌を優先的に引きます

### 牌の評価スコア

牌の有用性は以下のスコアで評価されます：

| 牌の番号または種類 | スコア | 説明 |
|---|---|---|
| 4, 5, 6（中張牌） | 20 | 最も有用（最も多角的に利用可能） |
| 3, 7 | 15 | 比較的有用 |
| 2, 8 | 10 | やや有用 |
| 1, 9（端牌） | 5 | オタ風、雀頭、チャンタなどの特定の役向け |
| 字牌（honor） | 12 | 中程度の有用性 |

## 実装方法

### 1. ルーム作成時の設定

WebSocketで`join`メッセージを送信する際に、`tsumoLuck`パラメータを指定します：

```json
{
  "type": "join",
  "payload": {
    "roomId": "ABC12345",
    "playerName": "太郎",
    "tsumoLuck": 2
  }
}
```

### 2. コンポーネント設計

#### GameRoom.js
- `setTsumoLuck(userId, luckLevel)`: プレイヤーのツモ運レベルを設定
- `getTsumoLuck(userId)`: プレイヤーのツモ運レベルを取得
- MahjongLogic初期化時に`tsumoLuckSettings`オプションを渡す

#### MahjongLogic.js
- `getTileScore(tile)`: 牌の有用性スコアを計算
- `drawTileWithLuck(userId)`: ツモ運を考慮した牌引き
- スコアベースの確率的選択メカニズム

#### server.js
- `handleJoin(ws, payload)`: join メッセージで `tsumoLuck` パラメータを受け付ける
- ツモ運設定をGameRoomに設定

## アルゴリズム詳細

### 牌選別アルゴリズム

```
drawTileWithLuck(userId):
  1. 壁（wall）から引き出し可能な全牌を列挙
  2. プレイヤーのツモ運レベルを取得
  
  if レベル == 0:
    ランダムに牌を選ぶ（補正なし）
  else:
    確率判定 (レベル1: 30%, レベル2: 70%)
    if 実用的な牌を選ぶ:
      各牌をスコアで重み付けし、確率的に選択（高スコア→高確率）
    else:
      ランダムに牌を選ぶ
  
  3. 選択された牌を壁から取り出して返す
```

### スコアベース確率選択

高スコアの牌ほど選ばれやすい確率的選択：

```
totalScore = Σ(各牌のスコア)
random = Math.random() * totalScore

for 各牌 of 牌リスト:
  random -= 牌のスコア
  if random <= 0:
    この牌を選択
```

## パフォーマンス影響

統計テストの結果：
- **レベル0**: 約31%の確率で良い牌（スコア≥20）を引く
- **レベル1**: 約35-40%の確率で良い牌を引く（検証予定）
- **レベル2**: 約42%の確率で良い牌を引く

## テスト

テストファイル: `backend/tests/test-tsumo-luck.js`

実行方法:
```bash
cd backend
node tests/test-tsumo-luck.js
```

テスト内容:
1. **Test 1**: 牌スコアリング（全スコアが期待値と一致）
2. **Test 2**: ツモ運設定の保存と取得
3. **Test 3**: 統計的な牌引き分布（ツモ運レベル2 > 0）

## 使用例

### JavaScript（フロントエンド）

```javascript
// ツモ運レベル2で参加
ws.send(JSON.stringify({
  type: 'join',
  payload: {
    roomId: 'ABC12345',
    playerName: 'Player1',
    tsumoLuck: 2  // 強い補正
  }
}));
```

### リーチと組み合わせた戦略

ツモ運モードを使用することで、リーチ後のツモで和了する確率が高まります：

- リーチ中は役がない牌を引くと自動的に捨てられます
- ツモ運レベルを上げることで、より実用的な牌を引き易くなり、和了確率が上昇

## 将来の改善案

1. **ゲーム中の動的調整**: ゲーム途中でツモ運レベルを変更可能にする
2. **UI統合**: フロントエンド画面でツモ運レベルを選択できるUIを追加
3. **スコアリング改善**: プレイヤーの手牌状況を考慮したより洗練されたスコアリング
4. **アーカイブ機能**: 過去のゲーム統計でツモ運の影響を記録・分析

## 注意事項

- ツモ運レベルはゲーム開始前（プレイヤー参加時）に設定する必要があります
- ゲーム中には変更できません（再設計が必要な場合は別途実装が必要）
- CPU対戦でもツモ運設定に対応しています

## 実装ファイル一覧

- `backend/src/logic/GameRoom.js` - ツモ運設定の管理
- `backend/src/logic/MahjongLogic.js` - 牌引きのコア実装
- `backend/src/server.js` - WebSocket メッセージ処理
- `backend/tests/test-tsumo-luck.js` - テストスイート
