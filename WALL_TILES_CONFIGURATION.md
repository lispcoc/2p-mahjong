# 壁牌設定の改善 - ゲーム作成時のルール設定

## 概要
ゲーム作成時の「壁の枚数」設定を改善し、配牌（配られた牌）を除外した、実際にツモできる壁牌のみを設定できるようにしました。

## 背景
従来：
- wallTiles設定: 30～136枚
- デフォルト: 70枚

この設定が全体の壁牌数を指すため、ユーザーが「配牌は関係なく、純粋にツモできる牌の枚数」を指定できませんでした。

## 改善内容

### 牌の構成分析
マージャンゲーム全体では136枚の牌が使用されます：

1. **配牌**: 27枚
   - 各プレイヤーに13牌ずつ配布（2プレイヤー＝26牌）
   - 親が追加で1牌ツモ（合計27牌）

2. **予約牌（ツモしてはいけない牌）**: 22枚
   - ドラ表示牌の候補: 4枚
   - ドラタイルの候補: 4枚
   - 裏ドラ表示牌の候補: 4枚
   - 裏ドラタイルの候補: 4枚
   - かん牌スペース（嶺上牌）: 3枚
   - かん牌補充用: 3枚

3. **実際にツモできる壁牌（wallTiles）**: 最大87枚
   - 計算: 136（全牌）- 27（配牌）- 22（予約牌）= **87枚**

### 設定の変更
**バックエンド** (server.js, GameRoom.js, MahjongLogic.js)
```javascript
const minWallTiles = 30;
const maxWallTiles = 87; // Updated for usable wall tiles (excluding deal and reserved)
```

**フロントエンド** (HomePage.tsx)
```typescript
// wallTiles: 配牌を除いた、ゲーム進行中にツモできる壁牌の枚数
// 計算: 全牌136枚 - 配牌27枚 - 予約牌22枚 = 最大87枚
const defaultWallTiles = 65
const minWallTiles = 30
const maxWallTiles = 87
```

### UI表示の改善
ルール設定モーダルのラベルと説明を以下のように更新：

**ラベル:**
- 旧: 「壁の枚数」
- 新: 「ツモ牌の枚数（配牌を含まない）」

**説明:**
- 「30〜87（通常 65）」
- 「配牌27枚と予約牌22枚を除いた、ゲーム進行中にツモできる牌の枚数です」

## ゲーム進行時の動作

### ゲーム開始時のフロー
1. ユーザーがルール設定で「ツモ牌の枚数」を指定（30～87）
2. MahjongLogic.initialize()で：
   - 全牌136枚を生成してシャッフル
   - 指定された枚数（デフォルト65枚）に切り詰め
3. dealTiles()で：
   - 配牌27枚をプレイヤーに配布
   - 壁の最後に予約牌22枚をセット
4. ゲーム中：
   - プレイヤーは最大65牌をツモできる
   - 流局判定: 壁の牌がなくなったら流局

## デフォルト値の設定根拠
- **最小（30枚）**: ゲームが早期に終わる短い対局向け
- **デフォルト（65枚）**: バランスの取れた平均的なゲーム
- **最大（87枚）**: ロングゲーム向け

通常のマージャンでは、全136枚から配牌と予約牌を除いた約87牌のうち、60～70牌程度がツモされることが多いため、デフォルト値の65は妥当です。

## 実装ファイル一覧
- `backend/src/server.js`: API層でのwallTiles検証
- `backend/src/logic/GameRoom.js`: ゲームルーム初期化
- `backend/src/logic/MahjongLogic.js`: マージャン牌の初期化とツモロジック
- `frontend/components/HomePage.tsx`: ユーザーインターフェース

## 互換性
- 既存のゲームルームは変更されません（新規作成時から適用）
- クライアント側でのバージョンアップのみで使用可能
