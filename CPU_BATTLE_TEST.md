# CPU自動対戦テストレポート

## テスト概要

このテストは、CPU同士を決着がつくまで戦わせることで、ゲームロジックの潜在的な問題を検出するものです。

## テストツールの使い方

```bash
# 1回のテストを実行
node backend/test-cpu-battle.js

# 複数回のテストを実行（例：5回）
node backend/test-cpu-battle.js 5

# 詳細ログを有効にする（test-cpu-battle.jsの編集が必要）
# verbose: true に設定
```

## 検出される問題

このテストは以下の問題を自動的に検出します：

### 1. 手牌枚数の異常
- **検出内容**: 手牌 + 副露の合計が13枚または14枚でない場合
- **原因**: ポン・チー・カンの処理で牌の追加・削除が正しく行われていない

### 2. 無限ループ
- **検出内容**: 最大ターン数（デフォルト300ターン）に達してもゲームが終了しない
- **原因**: ゲーム終了条件が正しく判定されていない、または壁牌が尽きても処理が続く

### 3. 処理のハング
- **検出内容**: 特定の状態でCPUの処理が停止する
- **原因**: 条件分岐のミス、エラーハンドリングの不備

## 既知の問題

### 問題1: ポン後の手牌枚数エラー（修正済み）

**症状**: 
```
❌ 異常な手牌枚数: CPU-1 - 手牌11枚 + 副露0枚 = 11枚
```

**原因**: 
テストコードで`melds`の構造を誤って理解していました。`melds`は`m.tiles.length`という構造ではなく、直接配列の配列（`m.length`）でした。

**修正**: 
test-cpu-battle.jsの67-73行目を修正し、`m.tiles.length`を`m.length`に変更しました。

### 問題2: テストコードのNullPointer例外（修正済み）

**症状**: 
```
TypeError: Cannot read properties of undefined (reading 'length')
```

**原因**: 
`melds`配列の要素がnullまたはundefinedの場合に、`.tiles`にアクセスしようとしていました。

**修正**: 
Nullチェックを追加し、安全にmeldsの枚数を計算するように修正しました。

## テスト結果の読み方

テスト終了時に以下の情報が表示されます：

```
========================================
  テスト結果
========================================

総ターン数: 45
ゲーム状態: finished

🎉 勝者: CPU-2

最終スコア:
  CPU-1: 24000点
  CPU-2: 26000点

✅ エラーは検出されませんでした

========================================
✅ テスト成功: ゲームは正常に完了しました
========================================
```

### 複数回実行の場合

```
==================================================
  総合結果
==================================================
総テスト数: 5
成功: 4
失敗: 1
未完了: 0
平均ターン数: 67.4

❌ 検出されたエラー数: 1
  1. 異常な手牌枚数: CPU-1 - 手牌11枚 + 副露0枚 = 11枚 (Turn: 10)

==================================================
```

## 推奨事項

1. **定期的なテスト実行**: 新機能を追加した後は必ずこのテストを実行してください
2. **複数回実行**: 問題は特定のランダムな牌の組み合わせでのみ発生することがあるため、最低10回以上実行することを推奨します
3. **ログの確認**: エラーが検出された場合は、詳細ログを有効にして再実行してください

## 今後の改善案

- [ ] ロン・ツモ和了の検証強化
- [ ] リーチ処理の検証
- [ ] フリテン処理の検証
- [ ] スコア計算の正確性検証
- [ ] 特定のシナリオ（七対子、国士無双など）の強制テスト

## 実装詳細

テストコードの場所: `backend/test-cpu-battle.js`

主な機能:
- `startGame()`: 新しいゲームを初期化
- `executeTurn()`: 1ターンを実行
- `checkGameState()`: 各ターン後にゲーム状態を検証
- `runUntilFinish()`: ゲーム終了まで自動実行
- `runMultiple(count)`: 複数回のテストを実行
